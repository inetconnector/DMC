# DMC C++ Reference

This directory contains the active C++ reference for deterministic
multiresolution context selection and exact selected-span attention.

Build:

```bash
cmake -S cpp -B cpp/build -DCMAKE_BUILD_TYPE=Release
cmake --build cpp/build -j
cpp/build/dmc_test
```
